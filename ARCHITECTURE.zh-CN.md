# 脱敏架构说明

本文介绍启发 `ed-web` 的私人 companion 项目整体架构，但不会公开真实课程数据、凭据或生产环境基础设施细节。

## 为什么会有两个实现

公开的 `ed-web` 刻意设计成纯浏览器端应用。它可以独立使用，也方便审计：认证信息留在浏览器，抓取内容留在 IndexedDB，不存在应用后端。

私人 companion 解决的是另一个问题：在不依赖浏览器一直开着的情况下，持续维护一个个人学习资料归档。这个目标需要持久化和定时任务，因此隐私边界与生产运维就更重要。

## 公开应用

```text
浏览器
  |
  | 直接发起经过认证的请求
  v
Ed Discussion API
  |
  v
标准化 / 整理帖子
  |
  +--> IndexedDB
  |
  +--> 搜索 + 筛选
  |
  +--> 规则式摘要 / 标签
  |
  v
静态界面
```

核心特点：

- 没有应用后端；
- token 只存在浏览器当前 session 中；
- 论坛内容只保存在浏览器本地 IndexedDB；
- API 由浏览器直接调用，数据流容易核对；
- 使用规则式摘要，不需要把课程内容发送给其他服务。

## 私人 companion

私人实现是在相同问题基础上扩展出的持久化数据管道：

```text
定时任务
   |
   v
Ed API 客户端
   |
   v
增量同步
   |
   v
SQLite 持久化
   |
   +-------------------+
   |                   |
   v                   v
可搜索归档          手动 AI 整理
   |                   |
   +---------+---------+
             |
             v
       静态 Dashboard 生成
             |
             v
        受访问控制的托管
```

### 增量同步

任务先获取轻量的帖子元数据，只有在相关更新时间或回复数量变化时，才重新抓取正文和回复。这样日常刷新只需要少量请求，而不是每次重新下载全部历史内容。

### SQLite 持久化

本地关系型数据库保存帖子、回复、元数据、生成摘要和处理状态。持久化后可以做长期搜索与复习，而不需要每次查询都重新访问原论坛。

### 定时任务

常规同步和静态站点生成自动运行。AI 辅助整理与日常任务严格分开，只能手动触发，因此普通刷新不会意外消耗 AI 配额，也不会在无人确认时进入 AI 工作流。

### 静态 Dashboard 生成

私人管道把当前状态渲染成静态 HTML。这样 Web 服务层非常简单：生成完成后，源站认证信息和敏感处理逻辑都不需要交给网页服务器。

## 安全与隐私边界

公开仓库会明确排除私人生产环境中的以下内容：

- API token 或认证 session；
- SSH 私钥、主机地址、端口、用户名或服务器别名；
- 生产文件系统路径或服务凭据；
- 密码文件或密码哈希；
- 原始课程/论坛导出数据或生成数据库；
- 私密帖子；
- 学生姓名及其他个人信息；
- 会议密码、签到码或其他受限制课程信息；
- 生产日志；
- 基于真实课程内容生成的私人学习笔记。

因此，这份架构说明可以展示软件工程能力，但不会让私人生产系统和真实数据被复现出来。

## 展示的工程能力

公开版与私人版组合起来，可以体现这些能力：

- 浏览器端隐私设计与威胁建模；
- API 客户端设计与数据标准化；
- 增量同步；
- SQLite 持久化；
- 后台定时处理；
- 确定性任务与手动 AI 工作流分离；
- 静态站点生成；
- 敏感个人数据的访问控制；
- 在不暴露私人生产环境的前提下设计可公开使用的产品版本。

## 与 `ed-web` 的关系

`ed-web` 不是私人系统的 mock-up，而是用户工作流的另一个真正可运行实现，只是隐私模型更严格。两者解决的是同一个问题，但架构取舍不同：

| 关注点 | `ed-web` | 私人 companion |
| --- | --- | --- |
| 执行环境 | 浏览器 | 定时任务 + 静态站点 |
| 持久化 | IndexedDB | SQLite |
| Token 所在位置 | 仅浏览器 session | 受保护的本地运行时配置 |
| 摘要 | 确定性规则 | 规则 + 手动 AI 整理 |
| 托管 | 公开静态应用 | 受访问控制的私人 dashboard |
| 仓库中的课程数据 | 无 | 无 |

这种拆分让公开项目保持可用、可审计和可分享，同时也能说明私人学习工作流背后的完整工程设计。
